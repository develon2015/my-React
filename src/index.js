import './index.html';
import ReactDOM from 'react-dom';
import { StrictMode, } from 'react';
import App from './pages/App';

ReactDOM.render(<StrictMode><App></App></StrictMode>, /* container */ window.app);